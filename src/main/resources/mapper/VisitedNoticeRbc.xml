<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.bank.dgb.ibs.app.visited_notice.VisitedNoticeRbc">

	<resultMap id="visitedNoticeMap" type="kh.bank.dgb.ibs.app.visited_notice.VisitedNotice">
		<id property="userId" column="userid" />
		<result property="visitYn" column="visityn" />
		<result property="visitedDate" column="visitdate" />
		<result property="visitedTime" column="visittime" />
	</resultMap>

	<insert id="registerVisitedNotice">
		INSERT INTO visited_notify (userid, visityn, visitdate, visittime)
		VALUES (#{userId}, 'Y', TO_CHAR(NOW(), 'YYYYMMDD'), TO_CHAR(NOW(), 'HH24MISS000'))
	</insert>

	<select id="retrieveVisitedNotice" resultMap="visitedNoticeMap">
		SELECT userid, visityn, visitdate, visittime
		FROM visited_notify
		WHERE userid = #{userId}
	</select>

	<update id="updateVisitedNotice">
		UPDATE visited_notify
		SET visityn = 'Y',
			visitdate = TO_CHAR(NOW(), 'YYYYMMDD'),
			visittime = TO_CHAR(NOW(), 'HH24MISS000')
		WHERE userid = #{userId}
	</update>

</mapper>
