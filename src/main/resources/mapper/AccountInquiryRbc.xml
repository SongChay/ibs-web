<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.bank.dgb.ibs.app.local.account_inquiry.AccountInquiryRbc">

	<!--
		Placeholder query — replace with the real port of whichever mapper this feature
		corresponds to (see old BIZMOB_HOME/ibs/conf/mybatis/mapper/*.xml for source SQL).
	-->
	<select id="findBalanceByAccountNo" resultType="java.math.BigDecimal">
		SELECT balance
		FROM account
		WHERE account_no = #{accountNo}
	</select>

</mapper>
